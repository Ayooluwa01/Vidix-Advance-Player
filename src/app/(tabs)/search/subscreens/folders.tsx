import { StyleSheet, Text, View } from "react-native";

const folders = () => {
  return (
    <View>
      <Text>folders</Text>
    </View>
  );
};

export default folders;

const styles = StyleSheet.create({});
