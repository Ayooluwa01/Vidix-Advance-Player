import { VideoPlayerView } from "@/features/videoplayer";
import { StyleSheet, View } from "react-native";

const Index = () => {
  return (
    <View className="flex-1">
      <VideoPlayerView />
    </View>
  );
};

export default Index;

const styles = StyleSheet.create({});
